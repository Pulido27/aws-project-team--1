import machine
import time
led = machine.Pin(2, machine.Pin.OUT)
boton = machine.Pin(15, machine.Pin.IN, machine.Pin.PULL_UP)

while True:
    if boton.value() == 0:
        led.value(1)
    else:
        led.value(0)
time.sleep(0.1)